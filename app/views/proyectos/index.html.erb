<h1>Listing proyectos</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Nombre</th>
      <th>Hs previstas</th>
	  <th>Hs acumuladas</th>
	  <th>Responsable</th>
	  <th>Último comentario</th>
	  <th>Usuario</th>
	  <th>Timestamp</th>
    </tr>
  </thead>

  <tbody>
    <% @proyectos.each do |proyecto| %>
      <% responsable = @personas.find(participante.proyecto.persona_id) %>
	  <tr>
        <td><%= proyecto.id %></td>
		<td><%= proyecto.nombre %></td>
		<td><%= proyecto.horas %></td>
		<td><%= proyecto.entradas.map {|e| e.tiempo}.sum %></td>
		<td><%= responsable.nombre %></td>
		<% ent= proyecto.entradas.last %>
		<td><%= ent.comentario%></td>
		<td><%= @personas.find(ent.persona_id).nombre%></td>
		<td><%= ent.created_at %></td>
		<td><%= link_to 'Show', proyecto %></td>
        <td><%= link_to 'Edit', edit_proyecto_path(proyecto) %></td>
        <td><%= link_to 'Destroy', proyecto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
		<td><%= link_to 'New Entrada', new_proyecto_entrada_path(proyecto) %></td>
		
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Proyecto', new_proyecto_path %>
