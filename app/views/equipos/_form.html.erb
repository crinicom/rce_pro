<%= form_for(@equipo) do |f| %>
  <% if @equipo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@equipo.errors.count, "error") %> prohibited this equipo from being saved:</h2>

      <ul>
      <% @equipo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre, :autofocus => true %>
  </div>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :descripcion %>
  </div>
  
   <% personas = User.find(:all, :order => :name) %>
<p>Proyecto A: 
  <%= f.collection_select :responsable_id, personas, :id, :name%>
</p>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
